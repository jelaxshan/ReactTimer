import React, {Component, PropTypes} from "react";

class Timer extends Component {
  render() {
    return (
      <div>
        Timer page
      </div>
    );
  }
}

Timer.propTypes = {};

export default Timer;
